# EVENT LOOP

Allows Node.js to:
- Handle multiple requests, Without blocking the main thread, Using asynchronous, non-blocking I/O

📌 Node.js is single-threaded, but still fast because of the event loop